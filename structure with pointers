#include <stdio.h>
struct sample
{
    int a,b;
    
    
};
struct sample *s;
int main(){
    printf("enter 2 numbers");
    scanf("%d%d",&s->a,&s->b);
    int t=s->a+s->b;
    printf("sum=%d",t);
}
